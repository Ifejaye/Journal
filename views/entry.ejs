<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Journal App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">
    <style>
        .error-message {
            color: red;
            font-size: 0.9rem;
            margin-top: 0.2rem;
        }
    </style>
</head>
<body>
 

    <div class="container mt-4">
        <h1 class="text-center">Personal Journal</h1>
        <form id="journalForm" action="/submit" method="post" onsubmit="return validateForm()">
            <div id="journal-form" class="mt-4">
                <div class="mb-3">
                    <label for="emotion" class="form-label">Input Type</label>
                    <select 
                        name="contentType" 
                        class="form-select" 
                        id="emotion"
                        onchange="toggleLocationField()">
                        <option selected value="">What are you reporting?</option>
                        <option value="Hourly Reports">Hourly Reports</option>
                        <option value="Thoughts">Thoughts</option>
                        <option value="To-Do">To-Do</option>
                        <option value="😠">😠 Angry</option>
                    </select>
                    <div id="emotionError" class="error-message"></div>
                </div>

                <div class="mb-3">
                    <label for="thought" class="form-label">Thought</label>
                    <textarea 
                        name="content" 
                        class="form-control" 
                        id="thought" 
                        rows="3"></textarea>
                    <div id="thoughtError" class="error-message"></div>
                </div>

                <div class="mb-3" id="locationField" style="display: none;">
                    <label for="location" class="form-label"><Location></label>
                    <textarea 
                        name="location" 
                        class="form-control" 
                        id="location" 
                        rows="1"></textarea>
                    <div id="locationError" class="error-message"></div>
                </div>

                <button class="btn btn-primary" type="submit">Submit</button>
            </div>
        </form>
    </div>

    <script src="script.js"></script>
</body>
</html>
